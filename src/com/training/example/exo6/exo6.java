package com.training.example.exo6;

import java.util.ArrayList;
import java.util.Scanner;

public class exo6 {
    public static void main(String[] args) {
        /*int[][] array1 = {{0,18}, {1,45}, {45,48}, {-3,2}};
        int top = 0;
        for (int i = 0; i < array1.length; i++) {
            for (int j = 0; j < array1[i].length; j++) {
                if (array1[i][j] > top) {
                    top = array1[i][j];
                }
            }
        }
        System.out.println(top);*/

        /*ArrayList<String> noms = new ArrayList<String>();
        ArrayList<Double> notes = new ArrayList<Double>();
        double note = 0;
        Scanner scan = new Scanner(System.in);

        while (note != -1) {
            System.out.println("Saisissez une note");
            note = scan.nextDouble();
            if (note != -1) {
                notes.add(note);
                System.out.println("Saisissez un nom");
                String nom = scan.next();
                noms.add(nom);
            } else {
                break;
            }
        }

        for (int i = 0; i < noms.size(); i++) {
            System.out.println("Nom: " + noms.get(i) + ", note: " + notes.get(i));
        }*/
    }
}
